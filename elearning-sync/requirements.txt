requests>=2.28.0
PyYAML>=6.0
tqdm>=4.64.0
PySide6>=6.5.0
# PySide6's meta package normally pulls these in; keeping Addons explicit is
# important for conda/minimal installations because QtMultimedia lives here.
PySide6-Addons>=6.5.0
keyring>=24.0.0
pycryptodome>=3.18.0
win10toast>=0.9; sys_platform == 'win32'
# Office/PDF readers used by the in-app previewer (all are optional at runtime
# and have graceful fallbacks when a document uses an unsupported feature).
Pillow>=10.0
pillow-heif>=0.18.0
PyMuPDF>=1.24.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
odfpy>=1.4.1
pywin32>=306; sys_platform == 'win32'
# 可选：浏览器交互式登录（python -m playwright install chromium）
playwright>=1.40.0
